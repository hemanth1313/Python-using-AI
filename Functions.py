# Functions are just bundled  sets of instructions that we can repeatedly use without havint to write them again
# can pass data, known as parameters, into a function

pi = 3.14
#text = 'The value of pi is  ' + pi
text = 'The valie of pi is ' + str(pi) 
print(text)
